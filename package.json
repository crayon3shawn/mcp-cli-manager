{
  "name": "mcp-cli-manager",
  "version": "1.0.0",
  "description": "MCP Server Management Tool - A CLI tool for managing Model Context Protocol servers",
  "type": "module",
  "main": "bin/mcp-cli-manager",
  "bin": {
    "mcp": "bin/mcp-cli-manager"
  },
  "scripts": {
    "start": "node bin/mcp-cli-manager",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "git describe --tags --abbrev=0 || echo '0.0.0'",
    "prepack": "npm run version | xargs -I{} npm version {} --no-git-tag-version --allow-same-version"
  },
  "keywords": [
    "mcp",
    "cli",
    "server",
    "manager",
    "model-context-protocol"
  ],
  "author": "crayon3shawn",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/crayon3shawn/mcp-cli-manager.git"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.3",
    "commander": "^11.1.0",
    "inquirer": "^9.2.12"
  }
}
