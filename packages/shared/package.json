{
  "name": "@mcp-cli-manager/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities and types for MCP CLI Manager",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@mcp-cli-manager/config": "workspace:*",
    "@types/node": "^20.11.19",
    "@vitest/coverage-v8": "^1.2.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
} 